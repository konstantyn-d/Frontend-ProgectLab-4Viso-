# Supabase (frontend, direct-to-Supabase with the anon key).
# Copy to .env.local and fill in your project values.
# If these are left empty, the app runs on bundled demo data.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
